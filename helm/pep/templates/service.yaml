apiVersion: v1
kind: Service
metadata:
  name: {{ include "pep.fullname" . }}
  labels:
    {{- include "pep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pep.selectorLabels" . | nindent 4 }}
