apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nutsadmin.fullname" . }}-config
  labels:
    {{- include "nutsadmin.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Values.config | toYaml | nindent 4 }}
