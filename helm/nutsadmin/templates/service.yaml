apiVersion: v1
kind: Service
metadata:
  name: {{ include "nutsadmin.fullname" . }}
  labels:
    {{- include "nutsadmin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nutsadmin.selectorLabels" . | nindent 4 }}
