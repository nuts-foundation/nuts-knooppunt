apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nuts-knooppunt.fullname" . }}-test-connection"
  labels:
    {{- include "nuts-knooppunt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nuts-knooppunt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
