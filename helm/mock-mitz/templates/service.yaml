apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-mitz.fullname" . }}
  labels:
    {{- include "mock-mitz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-mitz.selectorLabels" . | nindent 4 }}
