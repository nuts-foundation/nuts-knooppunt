apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-vc-issuer.fullname" . }}
  labels:
    {{- include "mock-vc-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-vc-issuer.selectorLabels" . | nindent 4 }}
